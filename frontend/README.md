# Frontend

This is the React + TypeScript frontend for the live polling app.

## Local development

```bash
cd frontend
npm install
npm run dev
```

By default it talks to the backend on `http://localhost:5000` (see `VITE_API_URL` and `VITE_SOCKET_URL` in `.env`).
